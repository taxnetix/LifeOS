# LifeOS — Python development dependencies
#
# Installed into the same .venv.  Required by /selftest.
#   .venv/bin/pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.3,<9                # tools/py/tests — golden-file fixtures vs vault.example/
pytest-cov>=6.0,<7            # coverage on the parsers, where gaps are dangerous
ruff>=0.8,<0.14               # lint + format; single tool, no black/isort/flake8 stack
